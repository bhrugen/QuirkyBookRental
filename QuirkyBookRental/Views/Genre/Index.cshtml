@model IEnumerable<QuirkyBookRental.Models.Genre>
@{
    ViewBag.Title = "Index";
}

<h2>Genre</h2>
<a type="button" class="btn-primary btn btn-sm" href="@Url.Action("Create")">
    <span class="glyphicon glyphicon-plus"></span>
    <span>Create New</span>
</a>
